import React from "react";
import { render, fireEvent } from "@testing-library/react";
import { MoreDropdown } from "../MoreDropdown"

test("renders MoreDropdown component", () => {
  render(<MoreDropdown />);
});